<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/index.css">
  <title>Mini Forum</title>
</head>
<body>
  <%- include('header') %>

  <main class="container">
    <h2 class="text-1">Threads</h2>
    <ul class="ul-1">
      <% if (threads.length === 0) { %>
        <li class="li-1">No threads yet. Create the first one below!</li>
      <% } else { %>
        <% threads.forEach(thread => { %>
          <li class="li-1">
            <a class="a-1" href="/thread/<%= thread.id %>"><%= thread.title %></a>
            <small class="small-1">by <%= thread.author %></small>
          </li>
        <% }) %>
      <% } %>
    </ul>

    <h3 class="text-1">Create New Thread</h3>
    <form class="form-1" action="/thread" method="POST">
      <input class="input-1" type="text" name="title" placeholder="Thread Title" required>
      <input class="input-1" type="text" name="author" placeholder="Your Name" required>
      <button class="button-1" type="submit">Post Thread</button>
    </form>
  </main>

  <%- include('footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- ถ้าไฟล์ JS อยู่ใน public/js ให้ชี้แบบนี้ -->
  <script src="/js/script.js"></script>
</body>
</html>

